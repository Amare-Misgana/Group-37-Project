from django.urls import path

from . import views

app_name = "admin_staff"

urlpatterns = [
    # Add admin_staff URL patterns here. Left empty to satisfy Django include()
    # Example: path('', views.index, name='index'),
]
